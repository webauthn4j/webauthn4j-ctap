<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.unifidokey">

    <application
        android:name=".app.handheld.UnifidoKeyHandHeldApplication"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity
            android:name=".app.handheld.presentation.AboutActivity"
            android:label="@string/title_activity_about" />
        <activity
            android:name=".app.handheld.presentation.LockScreenActivity"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:theme="@style/AppTheme.FullscreenTheme" />
        <activity
            android:name=".app.handheld.presentation.MainActivity"
            android:label="@string/title_activity_credential_list"
            android:launchMode="singleTask"
            android:theme="@style/AppTheme.NoActionBar"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".app.handheld.presentation.SettingsActivity"
            android:label="@string/title_activity_settings" />
        <activity
            android:name=".app.handheld.presentation.RegistrationConsentDialogActivity"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:theme="@style/AppTheme.FullscreenTheme" />
        <activity
            android:name=".app.handheld.presentation.AuthenticationConsentDialogActivity"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:theme="@style/AppTheme.FullscreenTheme" />
        <activity
            android:name=".app.handheld.presentation.CredentialSelectorDialogActivity"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:theme="@style/AppTheme.FullscreenTheme" />
    </application>

</manifest>
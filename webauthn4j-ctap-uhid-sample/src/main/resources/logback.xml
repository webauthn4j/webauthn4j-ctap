<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Set default log level -->
    <root level="INFO">
        <appender-ref ref="CONSOLE" />
    </root>

    <!-- More verbose logging for UHID components -->
    <logger name="com.webauthn4j.ctap.uhid" level="DEBUG" />
    <logger name="com.webauthn4j.ctap.authenticator" level="INFO" />
</configuration>
